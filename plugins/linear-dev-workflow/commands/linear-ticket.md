---
description: "End-to-end Linear ticket to PR workflow — reads the ticket, plans, implements, reviews, and raises a PR"
argument-hint: "LINEAR_TICKET_URL"
---

Invoke the `linear-dev-workflow:linear-ticket` skill with the following Linear ticket URL:

$ARGUMENTS
